.include libsdm
.include libstream

# workaround for parser bug in mymake
CLEAN = ${.SUBDIRS:=/clean}
INSTALL = ${.SUBDIRS:=/install}

## Build libraries: ${.SUBDIRS}
all: ${.SUBDIRS}

## Clean libraries: ${.SUBDIRS}
clean: ${CLEAN}

## Install libraries: ${.SUBDIRS}
install : ${INSTALL}
