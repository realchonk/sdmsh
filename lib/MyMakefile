.include libsdm
.include libstream

## Build libraries: ${.SUBDIRS}
all: ${.SUBDIRS}

# workaround for parser bug in mymake
CLEAN = ${.SUBDIRS:=/clean}

## Clean libraries: ${.SUBDIRS}
clean: ${CLEAN}
